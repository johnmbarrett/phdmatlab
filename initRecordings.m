function recordings = initRecordings
    if exist('./initRecordings.mat','file')
        load('./initRecordings.mat','recordings');
        return;
    end

%     ind = 1;
% 	recordings(ind).index				=		ind;
% 	recordings(ind).dataFile			=		'test';
% 	recordings(ind).array				=		'rect';
% 	recordings(ind).minmax				=		[-2048 2048];
% 	recordings(ind).sampleRate			=		25000;
% 	recordings(ind).desc				=		'test';
%     recordings(ind).spikeFile           =       'test spikes';
%     recordings(ind).spont               =       true;
%     recordings(ind).stimElec            =       NaN;
%     recordings(ind).vSyncChannel        =       'A2';
	ind = 1;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Retina A';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Powered Splitter';
    recordings(ind).factors             =       {'period'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Retina A spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Retina A';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 2;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 1';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Passive Splitter';
    recordings(ind).factors             =       {'rect'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 1 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 1';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 3;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 2';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Passive Splitter';
    recordings(ind).factors             =       {'rect'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 2 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 2';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 4;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 3';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P20 wt Flashing Half Fields Powered Splitter';
    recordings(ind).factors             =       {'spatialPeriod' 'phase'};
    recordings(ind).groupStimuli        =       3;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 3 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).groupStimuli        =       2;
    recordings(ind).skipStimuli         =       1;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 3';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 5;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 4';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P20 wt Flashing Half Fields Powered Splitter';
    recordings(ind).factors             =       {'colour' 'phase'};
    recordings(ind).groupStimuli        =       3;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 4 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).groupStimuli        =       2;
    recordings(ind).skipStimuli         =       1;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 4';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
	ind = 6;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Spont 1';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Spontaneous Activity';
    recordings(ind).spikeFile           =       'Spont 1 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stimElec            =       NaN;
	ind = 7;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Spont 2';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Spontaneous Activity';
    recordings(ind).spikeFile           =       'Spont 2 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stimElec            =       NaN;
	ind = 8;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Spont 3';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Spontaneous Activity';
    recordings(ind).spikeFile           =       'Spont 3 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stimElec            =       NaN;
	ind = 9;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Spont 4';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Spontaneous Activity';
    recordings(ind).spikeFile           =       'Spont 4 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stimElec            =       NaN;
	ind = 10;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Spont 5';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Spontaneous Activity';
    recordings(ind).spikeFile           =       'Spont 5 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stimElec            =       NaN;
    ind = 11;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 5';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Powered Splitter';
    recordings(ind).factors             =       {'rect'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 5 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 5';
    recordings(ind).textureRect         =       [0 0 640 480];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 12;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 6';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Powered Splitter';
    recordings(ind).factors             =       {'rect'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 6 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 6';
    recordings(ind).textureRect         =       [0 0 640 480];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 13;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Stim 7';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Powered Splitter';
    recordings(ind).factors             =       {'rect'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Stim 7 spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Stim 7';
    recordings(ind).textureRect         =       [0 0 640 480];
    recordings(ind).vSyncChannel        =       'A2';
    ind = 14;
    recordings(ind).index               =       ind;
    recordings(ind).dataFile            =       'Baseline';
    ind = 15;
	recordings(ind).index				=		ind;
	recordings(ind).dataFile			=		'Retina B';
	recordings(ind).array				=		'rect';
	recordings(ind).minmax				=		[-2048 2048];
	recordings(ind).sampleRate			=		25000;
	recordings(ind).desc				=		'P22 wt Flashing Full Fields Powered Splitter';
    recordings(ind).factors             =       {'period'};
    recordings(ind).groupStimuli        =       2;
    recordings(ind).isCumulativeMaxT    =       false;
    recordings(ind).spikeFile           =       'Retina B spikes';
    recordings(ind).spont               =       true;
    recordings(ind).stepStimuli         =       2;
    recordings(ind).stimElec            =       NaN;
    recordings(ind).stimulusFile        =       'Retina B';
    recordings(ind).textureRect         =       [254 360 354 360];
    recordings(ind).vSyncChannel        =       'A2';
% 	ind = 4;
% 	recordings(ind).index				=		ind;
% 	recordings(ind).dataFile			=		'Stim1_e43';
% 	recordings(ind).stimulusFile		=		'stim_120615_01';
% 	recordings(ind).stimElec			=		73;
%     recordings(ind).gndElec				=		[];
% 	recordings(ind).array				=		'rect';
% 	recordings(ind).minmax				=		[-2048 2048];
% 	recordings(ind).sampleRate			=		25000;
% 	recordings(ind).nSamples			=		5001;
% 	recordings(ind).nZero				=		2501;
% 	recordings(ind).desc				=		'P90 CRX CNT-PDOT Normal Acsf';
    save('./initRecordings.mat','recordings');
end